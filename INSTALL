/******************************************/

    Installing libFSKDecoder

/******************************************/

Required developer packages:
----------------------------
* fftw3

Used libraries:
---------------
* fftw3f


Compiling:
----------
cd in your source directory
for a static library type:
  make static
for a shared library type:
  make shared

Using  libFSKDecoder
--------------------
In your application include FSKDecoder.h

Tested systems: 
* Linux PC
* Raspberry Pi
* ODROID X2

Porting to Windows is not tested. 
It should be possible, there are no Linux specials used.


/******************************************/

    Installing cmdfsk

/******************************************/

Used libraries:
---------------
* fftw3f
* libFSKDecoder - static build

Required developer packages:
----------------------------
* fftw3
* alsa


cd in your source directory and type
  make

The FSKDecoder.h and libFSKDecoder are expected in the directory ../libFSKDecoder

Tested systems: 
* Linux PC
* Raspberry Pi
* ODROID X2

Porting to Windows is not tested. Cmdfsk is using the Linux sound system "alsa", 
which does not exists on Windows. If you remove the sound input form program 
sources, it should be possible to compile for file inpult only.
